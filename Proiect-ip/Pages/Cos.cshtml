@page
@model Proiect_ip.Pages.CosModel
@{
    ViewData["Title"] = "Cosul de cumparaturi";
}

<div class="container mt-4">
    <h1 class="mb-4">Cosul de cumparaturi</h1>
    @if (Model.Produse.Count == 0)
    {
        <p class="text-muted">Cosul este gol.</p>
    }
    else
    {
        if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success">@TempData["SuccessMessage"]</div>
        }

        if (TempData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
        }

        <div class="row">
            @foreach (var item in Model.ProduseCos)
            {
                var cantitate = Model.Produse.Find(p => p.ProductId == item.IdProdus).Cantitate;
                <div class="col-md-4 mb-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">@item.Nume</h5>
                            <p class="card-text">Cantitate: @cantitate</p>
                            <div class="d-grid gap-2">
                                <a asp-page="/DetaliiProdus" asp-route-id="@item.IdProdus" class="btn btn-primary">Detalii</a>
                                <form method="post" asp-page-handler="RemoveFromCart" asp-route-productId="@item.IdProdus">
                                    <button type="submit" class="btn btn-danger">Sterge</button>
                                </form>
                                <form method="post" asp-page-handler="UpdateQuantity" asp-route-productId="@item.IdProdus">
                                    <div class="input-group">
                                        <input type="number" name="cantitate" value="@cantitate" class="form-control" min="1" required />
                                        <button type="submit" class="btn btn-primary">Actualizeaza</button>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
        <form method="post" asp-page-handler="CreateOrder">
            <button type="submit" class="btn btn-info">Trimite comanda</button>
        </form>
    }
</div>
